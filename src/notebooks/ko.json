{
  "# Case 01: Jev as a LangGraph router\n\nVerifies the routing graph against the live TypeSafe API and the configured chat model.\n\n1. One Jev request answers three independent questions about a message.\n2. Plain code turns those answers into a route, so the policy can change without another API call.\n3. The graph runs end to end. Only the `answer` route calls the chat model.": "# Case 01: LangGraph 라우터로서의 Jev\n\n실제 TypeSafe API와 설정된 채팅 모델로 라우팅 그래프를 검증합니다.\n\n1. Jev 요청 한 번으로 메시지에 대한 독립적인 질문 세 개에 답합니다.\n2. 일반 코드가 그 답을 경로로 바꾸므로, API를 다시 호출하지 않고도 정책을 바꿀 수 있습니다.\n3. 그래프를 처음부터 끝까지 실행합니다. `answer` 경로만 채팅 모델을 호출합니다.",
  "## 1. One request, three judgments\n\nIntent (Choice), urgency (Score), and prompt injection (Noul) are asked together.": "## 1. 요청 한 번, 판단 세 개\n\nintent(Choice), urgency(Score), 프롬프트 인젝션(Noul)을 함께 묻습니다.",
  "## 2. Policy lives in code\n\n`decide` reuses the judgments above. Retuning a threshold needs no new inference.": "## 2. 정책은 코드에 있습니다\n\n`decide`는 위에서 얻은 판단을 그대로 재사용합니다. 임계값을 다시 조정해도 새 추론이 필요 없습니다.",
  "## 3. The graph, end to end\n\nOnly routes that reach the chat model are slow. Hosted NIM calls can take a minute or more.": "## 3. 그래프를 처음부터 끝까지\n\n채팅 모델에 도달하는 경로만 느립니다. 호스티드 NIM 호출은 1분 이상 걸릴 수 있습니다.",
  "<!-- repeats -->\n## 4. How stable are the judgments?\n\nEach message goes through `triage` **10 times**. The table shows how often the runs agree and how much the numbers move (mean, then min to max).": "## 4. 판단은 얼마나 안정적인가?\n\n각 메시지를 `triage`에 **10번** 통과시킵니다. 표는 실행들이 얼마나 일치하는지, 그리고 수치가 얼마나 흔들리는지(평균, 이어서 `min to max` 범위)를 보여줍니다.",
  "## 5. More scenarios\n\n16 hand-written messages, **5 runs each**. The expected route is my own judgment, so a mismatch is a finding, not necessarily a bug. Korean messages are included.": "## 5. 더 많은 시나리오\n\n손으로 쓴 메시지 16개를 **각 5번** 실행합니다. 기대 경로는 작성자의 판단일 뿐이므로, 불일치는 반드시 버그가 아니라 하나의 발견입니다. 한국어 메시지도 포함했습니다.",
  "## 6. What does a route cost?\n\nThe whole graph, **5 runs per route**, on the configured chat model. Only `answer` calls it.": "## 6. 경로마다 비용이 얼마나 드는가?\n\n설정된 채팅 모델로 그래프 전체를 **경로마다 5번** 실행합니다. `answer`만 채팅 모델을 호출합니다.",
  "## Result\n\nThe first three sections show single runs. Sections 4 to 6 repeat them, so the tables show trends: how often the routes agree, where the expected route and the observed route differ, and what each route costs.": "## 결과\n\n앞의 세 섹션은 한 번씩 실행한 결과입니다. 4~6번 섹션은 이를 반복하므로, 표에서 경향을 볼 수 있습니다. 경로가 얼마나 자주 일치하는지, 기대 경로와 실제 경로가 어디서 다른지, 그리고 경로마다 비용이 얼마나 드는지입니다.",
  "# Case 02: Jev inside a Deep Agent\n\nVerifies both integration shapes with the live TypeSafe API.\n\n1. `JevGuardrailMiddleware` screens a message before the agent starts (no chat model needed).\n2. `verify_claim` returns a typed verdict with confidence (no chat model needed).\n3. The full guarded agent runs with the configured chat model.": "# Case 02: Deep Agent 안의 Jev\n\n실제 TypeSafe API로 두 가지 통합 방식을 모두 검증합니다.\n\n1. `JevGuardrailMiddleware`가 에이전트 시작 전에 메시지를 걸러냅니다(채팅 모델 불필요).\n2. `verify_claim`이 신뢰도가 붙은 타입 있는 판정을 돌려줍니다(채팅 모델 불필요).\n3. 가드레일이 붙은 전체 에이전트를 설정된 채팅 모델로 실행합니다.",
  "## 1. Guardrail middleware\n\nCalled directly, so this section needs no chat model.": "## 1. 가드레일 미들웨어\n\n직접 호출하므로 이 섹션에는 채팅 모델이 필요 없습니다.",
  "## 2. `verify_claim` tool\n\nThree claim and evidence pairs that should come back supported, contradicted, and unrelated.": "## 2. `verify_claim` 도구\n\n각각 supported, contradicted, unrelated가 나와야 하는 주장-근거 쌍 세 개입니다.",
  "## 3. The guarded agent, end to end\n\nDeep Agents adds a large system prompt and several model turns, so this is the slow cell.": "## 3. 가드레일이 붙은 에이전트, 처음부터 끝까지\n\nDeep Agents는 큰 시스템 프롬프트와 여러 번의 모델 턴을 더하므로 이 셀이 가장 느립니다.",
  "<!-- repeats -->\n## 4. Guardrail, repeated\n\nFour normal and four injection messages, **10 runs each**. A message is blocked when the injection probability is not below `Policy.injection_block` (0.8), the rule the middleware uses.": "## 4. 가드레일 반복 실험\n\n정상 메시지 4개와 인젝션 메시지 4개를 **각 10번** 실행합니다. 인젝션 확률이 `Policy.injection_block`(0.8) 미만이 아니면 차단하며, 미들웨어도 같은 규칙을 씁니다.",
  "## 5. `verify_claim`, repeated\n\nSix claim and evidence pairs, two per expected verdict, **10 runs each**.": "## 5. `verify_claim` 반복 실험\n\n주장-근거 쌍 여섯 개(기대 판정마다 두 개)를 **각 10번** 실행합니다.",
  "## 6. The guarded agent, repeated\n\nThe clean request and the injection request, **3 runs each**, on the configured chat model.": "## 6. 가드레일이 붙은 에이전트 반복 실험\n\n정상 요청과 인젝션 요청을 설정된 채팅 모델로 **각 3번** 실행합니다.",
  "## Result\n\nThe first three sections show single runs. Sections 4 to 6 repeat them, so the tables show trends: how often the guardrail blocks, how consistent the `verify_claim` verdicts are, and how the agent behaves across runs.": "## 결과\n\n앞의 세 섹션은 한 번씩 실행한 결과입니다. 4~6번 섹션은 이를 반복하므로, 표에서 경향을 볼 수 있습니다. 가드레일이 얼마나 자주 차단하는지, `verify_claim` 판정이 얼마나 일관적인지, 그리고 에이전트가 실행마다 어떻게 동작하는지입니다.",
  "# Case 04: does a chat model with structured output match Jev?\n\nA control experiment. The Case 01 triage asks Jev three questions about a message: intent, urgency, and prompt injection. Here **the same three questions, worded the same way**, go to chat models that must answer through LangChain structured output (a `TypedDict`). Both engines feed the same `decide()` policy, and every number below comes from one data set that a script collected.\n\n1. **Screen.** Every model and reasoning setting, 31 messages, 3 runs each. It shows which configurations work at all and how fast.\n2. **Main.** The configurations that passed, 60 messages, 5 runs each (Jev 20). The comparison uses the first 5 runs of Jev so that consistency is measured on equal terms.\n3. **Verdict.** For each claim, whether Jev is clearly better, clearly worse, or not distinguishable, from the intervals in the data.\n\nThe expected routes are my own judgment, so agreement between engines is reported next to accuracy.": "# Case 04: 구조화 출력을 쓰는 채팅 모델은 Jev와 같은가?\n\n대조 실험입니다. Case 01의 분류는 Jev에게 메시지에 대한 세 가지, 곧 intent, 긴급도, 프롬프트 인젝션을 묻습니다. 여기서는 **같은 세 질문을 같은 문구로** 채팅 모델에게 묻고, 답은 LangChain 구조화 출력(`TypedDict`)으로 받습니다. 두 엔진의 결과는 같은 `decide()` 정책에 들어가고, 아래 모든 수치는 스크립트가 수집한 하나의 데이터에서 나옵니다.\n\n1. **스크린.** 모든 모델과 추론 설정을 메시지 31개, 각 3회로 돌립니다. 어떤 구성이 동작하는지, 얼마나 빠른지 보여 줍니다.\n2. **본 실험.** 스크린을 통과한 구성을 메시지 60개, 각 5회(Jev는 20회)로 돌립니다. 비교에는 Jev의 처음 5회만 써서 일관성을 같은 조건에서 잽니다.\n3. **판정.** 각 주장에 대해 Jev가 분명히 낫거나, 분명히 못하거나, 구분되지 않는지를 데이터의 구간으로 판단합니다.\n\n기대 경로는 제 판단이므로, 정확도와 함께 엔진 사이의 일치도도 보고합니다.",
  "## 1. Screen: which configurations work\n\n`valid` is the share of calls that returned a usable answer. A configuration goes on to the main stage when at least 90% of its calls were usable and its median latency is 60 s or less. **The rule uses availability, never accuracy**, because choosing finalists by accuracy on the same messages would flatter them.": "## 1. 스크린: 어떤 구성이 동작하는가\n\n`valid`는 쓸 수 있는 답을 돌려준 호출의 비율입니다. 호출의 90% 이상이 쓸 만하고 중앙 지연이 60초 이하이면 본 실험으로 넘어갑니다. **이 규칙은 정확도가 아니라 가용성만 씁니다.** 같은 메시지에서 정확도로 최종 후보를 고르면 결과가 부풀려지기 때문입니다.",
  "## 2. Main: accuracy against the expected route\n\nThe share of runs that ended on the expected route, with a 95% interval from resampling whole messages (the messages, not the repeats, are what limit how far a result generalizes). Jev is in red.": "## 2. 본 실험: 기대 경로에 대한 정확도\n\n실행이 기대 경로로 끝난 비율이고, 95% 구간은 메시지 전체를 다시 뽑아 계산합니다(결과가 얼마나 일반화되는지를 제한하는 것은 반복이 아니라 메시지입니다). Jev는 빨간색입니다.",
  "## 3. Consistency and agreement with Jev\n\n- **Consistency** is the mean share of runs that agree with a message's most common route (1.0 means the same answer every time).\n- **Agreement** is the share of messages on which the configuration's most common route equals Jev's, and Cohen's kappa corrects that for chance.": "## 3. 일관성과 Jev와의 일치도\n\n- **일관성**은 각 메시지의 가장 흔한 경로와 같은 답을 낸 실행의 평균 비율입니다(1.0은 매번 같은 답).\n- **일치도**는 구성의 가장 흔한 경로가 Jev와 같은 메시지의 비율이고, Cohen의 kappa는 이를 우연에 대해 보정한 값입니다.",
  "## 4. Injection: attacks that slip through, benign messages refused\n\n`attack_passes` is the share of runs on the 12 attack messages that did **not** end in `refuse`. `benign_refused` is the share of runs on the 8 look-alike messages (\"ignore my previous email\", \"override the address\") that were refused by mistake. Both are errors, in opposite directions.": "## 4. 인젝션: 통과한 공격과 잘못 거절된 정상 메시지\n\n`attack_passes`는 공격 메시지 12개에서 `refuse`로 끝나지 **않은** 실행의 비율입니다. `benign_refused`는 공격과 비슷한 단어(\"이전 메일은 무시해 주세요\", \"주소를 override\")가 들어 있는 정상 메시지 8개가 잘못 거절된 비율입니다. 둘 다 오류이고 방향이 반대입니다.",
  "## 5. Cost: latency and tokens\n\nMedian and 95th percentile latency of usable calls, and mean tokens per call. Jev's token counts come from the SDK usage and the chat models' from their usage metadata; reasoning tokens are reported only by some providers.": "## 5. 비용: 지연과 토큰\n\n쓸 수 있는 호출의 중앙값과 95번째 백분위 지연, 그리고 호출당 평균 토큰입니다. Jev의 토큰은 SDK 사용량에서, 채팅 모델의 토큰은 사용량 메타데이터에서 가져오며, 추론 토큰은 일부 공급자만 보고합니다.",
  "## 6. Where the routes differ\n\nExpected route (rows) against the route chosen (columns), counted over runs. Jev first, then the most accurate and the least accurate chat configuration.": "## 6. 경로가 어디서 달라지는가\n\n기대 경로(행)와 선택된 경로(열)를 실행 횟수로 센 표입니다. Jev, 그다음 가장 정확한 채팅 구성과 가장 덜 정확한 채팅 구성 순서입니다.",
  "## 7. The original stability experiment\n\nThe Case 01 notebook sent five messages through Jev **10 times** each. This is the same experiment for every configuration that passed the screen. The share is how often a run agrees with the message's most common route; the last column counts the runs that ended on the expected route.": "## 7. 처음의 안정성 실험\n\nCase 01 노트북은 메시지 다섯 개를 Jev에 **각각 10번** 보냈습니다. 이것은 스크린을 통과한 모든 구성에 대해 같은 실험을 한 것입니다. 일치도는 실행이 그 메시지의 가장 흔한 경로와 같았던 비율이고, 마지막 열은 기대 경로로 끝난 실행의 비율입니다.",
  "## 8. Verdict\n\nComputed from the tables above. \"Clearly\" means the 95% intervals do not overlap; overlapping intervals are **no detected difference at this sample size**, not proof of equality.": "## 8. 판정\n\n위 표에서 계산한 값입니다. \"분명히\"는 두 95% 구간이 겹치지 않는다는 뜻입니다. 구간이 겹치면 **이 표본 크기에서 차이를 찾지 못했다**는 뜻이지 같다는 증거가 아닙니다.",
  "## Result\n\nSee the tables above. Every figure comes from `experiments/data/*.jsonl`, written by `experiments/run_experiment.py`; the same numbers come out of `python -m experiments.report`.": "## 결과\n\n위 표들을 보세요. 모든 수치는 `experiments/run_experiment.py`가 쓴 `experiments/data/*.jsonl`에서 나오며, `python -m experiments.report`로도 같은 값이 나옵니다."
}
